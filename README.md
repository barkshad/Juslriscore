

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
4..run npm install
5..npm install git
6..git clone project link: [provided]
7..git clone project 2.0:
8..npm run project 2.0 in [sandbox]
9..npm install git clone repo;
10..apt install git
11..apt update 
